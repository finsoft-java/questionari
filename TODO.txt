
per tutte le tabelline, l'utente deve capire che i dati si stanno caricando (rotellina che gira..)
-> metterei un attributo "loading" in ogni component

ELiminare il menu "Modifica dati" dell'utente, in alto a sinistra

Pagina progetti: se il il progetto e' "già modificato", il bottone "Apri" diventa "Apri (read only)" e il progetto non 
è modificabile in alcun modo (CHECK AFU)

Pagina questionari: se il il questionario e' "già modificato", il bottone "Apri" diventa "Apri (read only)" e il questionario non 
è modificabile in alcun modo (CHECK AFU)

C'è il problema degli errori "OK", vedi ad esempio "Il server ha dato una risposta non attesa, si consiglia di aggiornare la pagina"

*** WISHLIST *********************************************************************************

jwtinterceptor: rinominare "username" in "token" ....

tutti i vari "style" sarebbe bello spostarli dentro file css...

a fine lavori, mi piacerebbe riempire l'applicativo di tooltip, per spiegare all'utente ogni campo a che cosa serve, in particolare nella pagina compilazione questionario

